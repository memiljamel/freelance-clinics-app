<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- Disabled -->
    <item android:alpha="0.38" android:color="?attr/colorOnSurface" android:state_enabled="false" />

    <!-- Error -->
    <item android:color="?attr/colorError" app:state_error="true" />

    <!-- Indeterminate -->
    <item android:color="@color/red_500" app:state_indeterminate="true" />

    <!-- Checked -->
    <item android:color="@color/red_500" android:state_checked="true" />

    <!-- Unchecked -->
    <item android:alpha="0.60" android:color="?attr/colorOnSurface" android:state_checked="false" />
</selector>